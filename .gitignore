# Dependencies
node_modules/

# Build output
out/
dist/
*.vsix
*.tsbuildinfo

# Local debug logs
logs/

# Local marker and scratch files
*.tct

# Test artifacts and coverage
.vscode-test/
coverage/
test-fixtures/**/*.js
test-fixtures/**/*.js.map

# Manual Kanban test workspace (fixtures + annotation store), not part of the product
.test-workspace/

# Editor & OS
.DS_Store
Thumbs.db
*.log
*.swp
*~

# Secrets
.env
.env.*

# This extension's own annotation data store, when this repo is the workspace
.out-of-code-insights/

.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/settings.json
!.vscode/extensions.json

# Orchestrator runtime state
.orchestra/
data/
.claude/
reports/mission-*.json

# Test runtime artifacts (regenerated by integration tests via ensureFixture)
test-fixtures/.vscode/settings.json
test-fixtures/image.png
# The lot7 clipboard suite recreates every one of its fixtures through
# `ensureFixture` at the start of a run and then saves its cut/paste edits back
# to disk, so tracking them only guarantees a dirty tree after `npm test`.
test-fixtures/lot7-*
test-fixtures/lot8-*.md
test-fixtures/lot9-*.md
test-fixtures/lot10-*.md
test-fixtures/docs/

test-fixtures/lot10-*.ts
test-fixtures/lot5-drag-*

# Agent worktrees
.claude/worktrees/
test-fixtures/lot11-*.ts
test-fixtures/lot12-*
test-fixtures/lot13-*
test-fixtures/lot14-*
.lot14-data-*
test-fixtures/lot15-*.md
test-fixtures/lot18-*
test-fixtures/CLAUDE.md
test-fixtures/AGENTS.md
.jackaiagent/
todo.md
